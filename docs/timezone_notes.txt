Memory Note: Refactoring timezone architecture to rely on user-level display timezones while preserving UTC-based security checks and anti-cheat validation. For React Native, avoided standard `Intl.supportedValuesOf('timeZone')` due to Hermes' partial Intl support, instead injecting a static list.
Memory Note: Display timezone updates must use upload.any() when handling multipart forms with multer since ManagerProfile may append avatars but also handles the primary text fields.
Memory note: Added timezone management best practices for treating localized DB timestamps securely as floating time using UTC parse/format methods to avoid offset shift errors, enforcing 'Z' ISO suffixes locally for uniformity.
Memory note: Live clocks correctly refactored using performance.now() elapsed calculation mapping from initTime + serverTimeOffset. Prevented device sleep debt skew manually mutating shadow clocks via setInterval increments.
Memory note: Added anti-cheat time validation in server's timeManager to block client requests with >60s offset. Both Web and Mobile clock translators sync with server offset to maintain monotonic stability and unified UTC standards.
